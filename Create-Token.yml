---
- hosts: all
  gather_facts: true
  tasks:
    - set_fact:
        var123: " "
    - debug:
        var: var123
    - debug:
        msg: "{{ ansible_hostname }} is the localhost and sophos_version is "{{ lookup('env','sophos_version') }}""
    - set_stats:
        data:
          Token: "token1234"
          next_var: "Sophos Installation has started"
    - set_fact:
        data1: 0
    - debug:
         msg: "Not Matched"
      when: not data1 == 1
    - set_fact:
        data1: 2
    - debug:
        msg: "Matched"
      when: data1 == 2
    - name: "Include the role"
      include_role:
        name: role_rename_disk_drives
      tags: disk

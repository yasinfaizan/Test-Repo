---
- hosts: all
  gather_facts: true
  tasks:
    - debug:
        msg: "{{ ansible_hostname }} is the localhost"
    - set_stats:
        data:
          Token: "token1234"
          next_var: "Sophos Installation has started"
    - set_fact:
        data1: 0
    - debug:
         msg: "Not Matched"
      when: not data1 == 1
